/*const bcrypt = require('bcrypt-nodejs')

function cryptPass(pass) {
	return bcrypt.hashSync(pass, bcrypt.genSaltSync(10))
}

function checkPass(pass, passHash) {
	return bcrypt.compareSync(pass, passHash)
}
*/

module.exports = {
	//cryptPass,
  //checkPass,
}
